## Docker Image

The application is available as a Docker image.

**Image:** `chetnnn/hey-nodejs:0.0.1`

### Run the Container

```bash
docker run -p 3000:3000 chetnnn/hey-nodejs:0.0.1
```

Then open in your browser:

```
http://localhost:3000
```
